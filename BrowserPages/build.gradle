/**task buildWebApp(type: Exec) {
    executable = "grunt"
    args = ["build"]
}**/

task buildWebApp(type: Exec) {
    workingDir '/Users/mattgaunt/Programming/Workspace/webview-browser/BrowserPages/'

    //on windows:
    commandLine 'grunt.exe', 'build'

    //on linux
    commandLine 'grunt', 'build'
}